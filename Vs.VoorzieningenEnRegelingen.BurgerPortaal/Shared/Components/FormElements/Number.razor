@inherits FormElementBase
@* MatNumericUpDownField
    MatNumericUpDownField Doesn't work the same in firefox as for google and in google it is a nightmare
     That is that Chrome will allow a . or a , depending on the culture of the INSTALLED VERSION of the browser.
     i.e. if you use an English browser in a Dutch culture it will not allow , but only .
     This is why it is better to implement number logic ourselves rather then use a numeric field*@
<MatStringField Label="@HttpUtility.HtmlDecode(_data.Label)" @bind-Value="@Value" Id="@_data.Name" Disabled="@_data.IsDisabled" HelperText="@HttpUtility.HtmlDecode(_data.HintText)" HelperTextPersistent=true />
<ValidationMessage For="@(() => _data.Value)" />
