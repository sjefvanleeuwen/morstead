@inherits FormElementBase
<WrapperDefault IsValid="@_data.IsValid">
    <Label Name="@_data.Name" Text="@_data.Label" TagText="@_data.TagText" />
    <FormElementHint Name="@_data.Name" Text="@_data.HintText" />
    <Error Text="@_data.ErrorText" />
    @* type="number"
        type=number Doesn't work the same in firefox as for google and in google it is a nightmare
         That is that Chrome will allow a . or a , depending on the culture of the INSTALLED VERSION of the browser.
         i.e. if you use an English browser in a Dutch culture it will not allow , but only .
         This is why it is better to implement number logic ourselves rather then trust the type*@
    <input required="@_data.IsRequired" disabled="@_data.IsDisabled" id="@_data.Name" type="text" aria-describedby="hint_@_data.Name" class="input__control input__control--text @_data.ElementSize" @bind="@Value">
</WrapperDefault>